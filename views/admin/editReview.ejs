<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Review</title>
  
</head>
<body>

<h2>Create Review</h2> 
<form action="/admin/editreview/<%= reviewData._id %>" method="POST">

    <label for="user">User</label><br> 
    <input type="text" name="user" placeholder="Enter username" value="<%= reviewData.user %>" required> <br>

    <label for="category">Category</label> <br>
    <select name="category" id="" required>
        <option value="">Please select the category</option>
        <% for (let i = 0; i < category.length; i++) { %>
          <option value="<%= category[i]._id %>" <%= reviewData.category.toString() === category[i]._id.toString() ? 'selected' : '' %>><%= category[i].title %></option>
        <% } %>
      </select> <br>

    <label for="product">Product</label> <br> 
    <select name="product" id="" required>
        <option value="">Select the product</option>
        <% for( let i = 0; i < product.length; i++ ) { %>
           <option value="<%= product[i]._id %>" <%= reviewData.product.toString() === product[i]._id.toString() ? 'selected' : '' %>><%= product[i].name %></option>
        <% } %>
    </select> <br>

    <label for="rating">Rating</label> <br>
     <select name="rating" id="">
        <option value="" disabled selected>Select the option</option>
        <option value="1" <%= reviewData.rating === 1 ? 'selected' : '' %>>1</option>
        <option value="2" <%= reviewData.rating === 2 ? 'selected' : '' %>>2</option>
        <option value="3" <%= reviewData.rating === 3 ? 'selected' : '' %>>3</option>
        <option value="4" <%= reviewData.rating === 4 ? 'selected' : '' %>>4</option>
        <option value="5" <%= reviewData.rating === 5 ? 'selected' : '' %>>5</option>
     </select> <br>
 
   
    <label for="review">Review</label> <br>
    <textarea name="review" id="review" placeholder="Enter your review"  required><%= reviewData.review %></textarea> <br>

    <button type="submit">Submit</button>
</form>






</body>
</html>
